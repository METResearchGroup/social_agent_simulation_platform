# Notes

We were trying to figure out the dynamics of executing the actions and what we have is OK, but I realize that how `oasis` does it is just better and more fleshed out. As I was doing the design, I realized that I was recreating already-available components.

For this initial batch of experiments, I'll simulate a full experiment with `oasis` to see how it works.

The flow of an experiment would be:

- Start of the experiment:
  - Define the agents used (we can just hardcode these for now in the experiment)
  - Define the algorithm used.

...
