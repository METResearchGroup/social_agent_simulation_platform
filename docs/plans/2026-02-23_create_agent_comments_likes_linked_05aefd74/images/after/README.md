# After screenshots

Capture manually:
1. Create agent form with comments, liked URIs, and linked agents filled
2. View agents showing the newly created agent (with persisted history)
3. Form submit success state

Run `cd ui && npm run dev`, create an agent with History and Link sections filled, submit, verify View agents shows the new agent.
