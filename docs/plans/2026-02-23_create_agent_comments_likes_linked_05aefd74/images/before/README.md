# Before screenshots

Capture manually:
1. Create agent form (collapsed History and Link sections)
2. Create agent form (expanded History section with comments, liked URIs)
3. Create agent form (expanded Link to existing agents section)

Run `cd ui && npm run dev`, open Create agent tab, expand/collapse sections as needed.
